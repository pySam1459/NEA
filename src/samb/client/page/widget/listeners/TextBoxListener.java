package samb.client.page.widget.listeners;

import samb.client.page.widget.TextBox;

public interface TextBoxListener {
	// This interface is called by a TextBox when the enter key is pressed
	
	public void onEnter(TextBox tb);

}
